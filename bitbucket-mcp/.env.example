# Bitbucket MCP Server Configuration

# Required: Your Bitbucket workspace name
# This is the workspace slug from your Bitbucket URL
# Example: https://bitbucket.org/YOUR-WORKSPACE/repo-name
BITBUCKET_WORKSPACE=hmuraj

# Authentication Option 1: App Password (recommended)
# Create an app password at: https://bitbucket.org/account/settings/app-passwords/
# Required permissions: Repositories (Read), Pipelines (Read)
BITBUCKET_USERNAME=your-username
BITBUCKET_PASSWORD=your-app-password

# Authentication Option 2: Personal Access Token
# Uncomment the line below and add your token if using token authentication
# BITBUCKET_TOKEN=your-access-token

# Optional: Bitbucket API URL (defaults to https://api.bitbucket.org/2.0)
# Only change this if you're using a self-hosted Bitbucket instance
# BITBUCKET_API_URL=https://api.bitbucket.org/2.0
